# Normalise line endings: LF in the repository and in the working tree,
# so Windows checkouts and Linux build servers agree.
* text=auto eol=lf

# Binary assets — never touch these.
*.pdf binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
